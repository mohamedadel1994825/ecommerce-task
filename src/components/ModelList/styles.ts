import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    listStyle: {
        marginBottom:30
    },
    modelTitle: {
        fontSize: 13,
        fontWeight: '500',
        marginBottom: 15,
        marginTop: 18,
        marginLeft: 15,
    },
    modelListContainer: {
        paddingHorizontal: 15,
        marginBottom: 30,
    },
});
