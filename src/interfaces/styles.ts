import {ViewStyle, ImageStyle, TextStyle} from 'react-native';

export interface AppStyles {
  centered: ViewStyle;
  flex: ViewStyle;
  image: ImageStyle;
  row: ViewStyle;
  emptyImageStyle: ImageStyle;
  emptyText: TextStyle;
  emptyList: ViewStyle;
}
