export interface Model {
    item: {
        id: string;
        name: string;
        image: string;
        availableProducts: string;
        quantity: number;
        space: string;
        color: string;
    };
}
